# URL du backend API (par défaut http://localhost:3000)
# En cas d'erreur "Network Error", vérifiez que le backend est démarré :
#   cd backend && npm run start:dev
VITE_API_URL=http://localhost:3000
